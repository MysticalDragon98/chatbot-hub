import { StringPreviewPipe } from './string-preview.pipe';

describe('StringPreviewPipe', () => {
  it('create an instance', () => {
    const pipe = new StringPreviewPipe();
    expect(pipe).toBeTruthy();
  });
});
